# =============================================================================
# LMS Academy WebApp — Environment Variables
# Copy this file to .env (development) or .env.production (production build)
#   cp .env.example .env
#   cp .env.example .env.production
# =============================================================================

# Django backend origin (no trailing slash). Used for API calls and media URLs.
VITE_BACKEND_URL=http://43.205.127.39:8000

# Optional: port for PM2 static server (ecosystem.config.cjs)
PORT=3000
